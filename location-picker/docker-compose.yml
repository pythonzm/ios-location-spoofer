services:
  location-picker:
    build: .
    container_name: location-picker
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - TOKEN=${TOKEN:?TOKEN is required}
      - PORT=${PORT:-8080}
      - DATA_FILE=/data/loc.json
      - AMAP_KEY=${AMAP_KEY:-}
    volumes:
      - .:/data
    restart: unless-stopped
